#include <stdio.h>
int main(){
  int a, b;
  printf ("Input length: ");
  scanf ("%d", &a);
  printf ("Input width: ");
  scanf ("%d", &b);
  int P = (a+b)*2;
  int S = a*b;
  printf ("Perimeter of the rectangle: %d\n", P);
  printf ("Area of the rectangle: %d", S);
  getch();
  return 0;
}
