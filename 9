#include <stdio.h>
int main (){
	int N, so1, so2, Sum;
	printf ("Input: N=");
	scanf ("%d", &N);
	
	if (N>9 && N<100){
		so1=N/10;
		so2=N%10;
		Sum= so1 + so2;
		printf ("Output: %d+%d=%d", so1, so2, Sum);
	} else {
		printf ("Error!!!");
	}
	
	getch();
	return 0;
}
